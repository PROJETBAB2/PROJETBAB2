/**
 * Identifiants intégrés au dépôt (local + mise en ligne).
 * Même contenu que frontend/src/devTestCredentials.ts — à garder aligné.
 * À remplacer par des secrets propres avant un usage réel grand public.
 */
export const DEV_TEST_RESTAURATEUR = {
  email: "admin@test.local",
  password: "TestResto2026!",
  name: "Admin test",
  locale: "fr",
} as const;
