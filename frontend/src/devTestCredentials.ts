/**
 * Préremplissage du formulaire restaurateur (local + build / mise en ligne).
 * Même contenu que backend/src/devTestCredentials.ts — à garder aligné.
 */
export const DEV_TEST_RESTAURATEUR = {
  email: "admin@test.local",
  password: "TestResto2026!",
} as const;
